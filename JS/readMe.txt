-> Download the three files and put them in a directory on your computer

-> Start by running "npm install prompt" in the root directory

-> To install the CLI globally: run "npm install -g" in the root directory of the project

-> To run the CLI : simply type "scrim" in a terminal (or "node main.js" if you didn't install it globally)
